# CMakeList.txt : CMake project for Vektory, include source and define
# project specific logic here.
#

# Add source to this project's executable.
add_executable (Vektory "Vektory.cpp" "Vektory.h" "VectorMath.h" "VectorMath.cpp")

if (CMAKE_VERSION VERSION_GREATER 3.12)
  set_property(TARGET Vektory PROPERTY CXX_STANDARD 20)
endif()

# TODO: Add tests and install targets if needed.
